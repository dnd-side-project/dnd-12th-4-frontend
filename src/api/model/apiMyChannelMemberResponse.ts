import type { Result } from "./result"
import type { MyChannelMemberResponse } from "./myChannelMemberResponse"

export interface ApiMyChannelMemberResponse {
  result?: Result
  body?: MyChannelMemberResponse
}
